name: Cineleaf
options:
  bundleIdPrefix: org.cineleaf
  deploymentTarget:
    macOS: "14.0"
  createIntermediateGroups: true
  generateEmptyDirectories: true
packages:
  CineleafCore:
    path: Packages/CineleafCore
settings:
  base:
    SWIFT_VERSION: "5.0"
    MACOSX_DEPLOYMENT_TARGET: "14.0"
    CLANG_ENABLE_MODULES: YES
    ENABLE_HARDENED_RUNTIME: YES
    GCC_WARN_INHIBIT_ALL_WARNINGS: NO
    SWIFT_TREAT_WARNINGS_AS_ERRORS: YES
targets:
  CineleafCLI:
    type: tool
    platform: macOS
    sources:
      - path: CineleafCLI
      - path: Cineleaf/Services/BoundedCache.swift
      - path: Cineleaf/Services/CineleafVideoCompositor.swift
      - path: Cineleaf/Services/CompositionBuilder.swift
      - path: Cineleaf/Services/CoreMediaBridge.swift
      - path: Cineleaf/Services/ExportService.swift
      - path: Cineleaf/Services/LocalDiagnostics.swift
      - path: Cineleaf/Services/MediaAccessManager.swift
      - path: Cineleaf/Services/MediaDerivativeStore.swift
      - path: Cineleaf/Services/MediaInspector.swift
      - path: Cineleaf/Services/ReverseMediaService.swift
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: org.cineleaf.CineleafCLI
        PRODUCT_NAME: CineleafCLI
        GENERATE_INFOPLIST_FILE: YES
    dependencies:
      - package: CineleafCore
  Cineleaf:
    type: application
    platform: macOS
    sources:
      - path: Cineleaf
        excludes:
          - Info.plist
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: org.cineleaf.Cineleaf
        PRODUCT_NAME: Cineleaf
        MARKETING_VERSION: 0.2.0
        CURRENT_PROJECT_VERSION: 1
        INFOPLIST_FILE: Cineleaf/Info.plist
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
        CODE_SIGN_ENTITLEMENTS: Cineleaf/Cineleaf.entitlements
        GENERATE_INFOPLIST_FILE: NO
        LD_RUNPATH_SEARCH_PATHS: "$(inherited) @executable_path/../Frameworks"
    dependencies:
      - package: CineleafCore
    scheme:
      testTargets:
        - CineleafTests
        - CineleafUITests
      gatherCoverageData: true
  CineleafTests:
    type: bundle.unit-test
    platform: macOS
    sources:
      - path: CineleafTests
    dependencies:
      - target: Cineleaf
      - target: CineleafCLI
      - package: CineleafCore
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: org.cineleaf.CineleafTests
        GENERATE_INFOPLIST_FILE: YES
  CineleafUITests:
    type: bundle.ui-testing
    platform: macOS
    sources:
      - path: CineleafUITests
    dependencies:
      - target: Cineleaf
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: org.cineleaf.CineleafUITests
        GENERATE_INFOPLIST_FILE: YES
